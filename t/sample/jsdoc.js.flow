/* @flow
 * @generated by TsFlower
 */
/** Toplevel jsdoc (keep) */
/*! Pinned comment (keep) */
/** Here's some jsdoc (keep) */
export type T = number;/** more on T (keep) */

export type U = "a" | "b"/** b (keep) */ | "c";

export type U1 = /** a (keep) */
/** a (keep) */
"a" | "b"/** b (keep) */ | "c";

export type U2 = "a" | "b"/** b (keep) */ | "c";
export type U3 = "a" | "b"/** b (keep) */ | "c";

export type LongLengthyProfuselyVerboselyNamedTypeCalledT</** JsDoc on type parameter S (keep) */
S> = { x: { y: { /**
       * z of the y.
       *
       * Details about z
       * (keep)
       */
z: S, ... }, ... }, ... };

export type S = LongLengthyProfuselyVerboselyNamedTypeCalledT<{ /** Some jsdoc on this property (keep) */
a1: LongLengthyProfuselyVerboselyNamedTypeCalledT<{
    /**
             * Some jsdoc on this inner property.  How does it indent? (keep)
             */
    x: number,
    y: void,
    ...
}>, ... }>;

/** Stray jsdoc (keep) */
/**
 * A function.
 *
  @param x an argument
  
  (keep)
 */
declare export function f(
    /** About parameter x (keep) */
    x: string
): any;
